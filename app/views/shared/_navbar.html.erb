<nav class="navbar navbar-expand-sm bg-dark navbar-dark sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">CS3300 Application</a>

    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
          Account <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
          <% if user_signed_in? %>
            Logged in as <strong><%= current_user.email %></strong>.
            <li> <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link dropdown-item' %> </li>
            <li> <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link dropdown-item'  %> </li>
          <% else %>
            <li> <%= link_to "Sign-In", new_user_registration_path, :class => 'navbar-link dropdown-item'  %> </li>
            <li> <%= link_to "Sign-Up", new_user_session_path, :class => 'navbar-link dropdown-item'  %> </li>
          <% end %>
        </ul>
      </li> 

      <li class="nav-item">
      <a class="nav-link" href="#" role="button">
        Export
      </a>
      </li>

    </ul>
  </div>
</nav>